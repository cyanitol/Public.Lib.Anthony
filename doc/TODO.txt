# Anthony - Pure Go SQLite Implementation - TODO
#
# Code Quality: Cyclomatic complexity <= 9 for all production code
# Coverage Snapshot: ~75% feature parity vs reference SQLite
# Last Updated: 2026-03-16

================================================================================
HIGH PRIORITY COMPLETION TARGETS (Production Readiness)
================================================================================

[x] Foreign Key Runtime Enforcement (83/83 tests passing)
    - [DONE] Wire FK manager through VDBE context
    - [DONE] INSERT FK validation (ValidateInsert) - blocks invalid child inserts
    - [DONE] DELETE FK validation (ValidateDelete) - blocks parent deletes when referenced
    - [DONE] UPDATE FK validation (ValidateUpdate) - blocks parent updates when referenced
    - [DONE] ON DELETE/UPDATE CASCADE - child rows auto-deleted/updated
    - [DONE] ON DELETE/UPDATE SET NULL - FK columns set to NULL
    - [DONE] ON DELETE/UPDATE SET DEFAULT - FK columns set to default
    - [DONE] RESTRICT action (same as NO ACTION for immediate constraints)
    - [DONE] Multi-column FK constraints (table-level FOREIGN KEY clauses)
    - [DONE] INTEGER PRIMARY KEY handling in FK lookups (rowid alias)
    - [DONE] Quoted table name handling
    - [DONE] Recursive CASCADE for self-referencing FKs
    - [DONE] PRAGMA foreign_key_check (with driverRowReader)
    - [DONE] DROP TABLE reference checking
    - [DONE] Unique index column order flexibility
    - [DONE] Deferred constraint checking at transaction boundaries
    - [DONE] Collation-aware FK matching
    - [DONE] Type affinity in FK comparison

[x] WITHOUT ROWID Tables (51 tests -> 45 passing, 6 skipped)
    - [DONE] Implement alt btree layout using PRIMARY KEY as key (no rowid)
    - [DONE] Route DML/DDL paths correctly; ensure index selection and VACUUM support
    - [DONE] Split/rewind regression: statement cache invalidation on root page changes
    - [DONE] rowid/oid/_rowid_ column rejection for WITHOUT ROWID tables
    - [DONE] FK validation integration for WITHOUT ROWID inserts
    - [DONE] JOIN queries with WITHOUT ROWID tables (cursor handling fixed)
    - [DONE] ROLLBACK for WITHOUT ROWID btrees (pager callback syncs btree cache)
    - [DONE] CASCADE FK actions: proper PK extraction with PKColumnIndices

[x] Window Functions (All ranking and value functions working)
    - [DONE] ROW_NUMBER() with streaming and partition modes
    - [DONE] RANK() with OpWindowRank opcode integration
    - [DONE] DENSE_RANK() with OpWindowDenseRank opcode
    - [DONE] NTILE() bucket distribution
    - [DONE] LAG/LEAD with offset parameter support
    - [DONE] FIRST_VALUE/LAST_VALUE frame access
    - [DONE] ROWS/RANGE frame specification parsing and extraction

[~] FTS5 Virtual Table (module implemented, 128 tests pass)
    - [DONE] Tokenizer pipeline (SimpleTokenizer, StopWordTokenizer)
    - [DONE] Inverted index with posting lists
    - [DONE] BM25/TFIDF ranking, snippet/highlight
    - [DONE] Query parser (AND, OR, NOT, prefix, phrase)
    - [DONE] Module registration in driver
    - [DONE] SQL parser support for CREATE VIRTUAL TABLE
    - [DONE] Virtual table instance creation on CREATE VIRTUAL TABLE
    - [DONE] Shadow table infrastructure (persistence.go)
    - [~] Persistent storage to database file (deferred, in-memory only)

[x] R-Tree Virtual Table (module implemented, all tests pass)
    - [DONE] R*-tree insertion/split/reinsertion
    - [DONE] 2D/3D/4D/5D spatial indexing
    - [DONE] Bounding box queries (overlap/contains/within)
    - [DONE] Nearest neighbor search (k-NN)
    - [DONE] Module registration in driver
    - [DONE] SQL parser support for CREATE VIRTUAL TABLE
    - [ ] Persistent storage to database file

[ ] Durability & Conformance Hardening
    - Run sqllogictest subsets vs reference SQLite; diff drift
    - Fuzz parser and record decoder; add crash-replay tests for WAL/journal
    - Target ≥80% coverage in pager/vdbe/btree/parser/engine

================================================================================
SCAFFOLDING CODE - Prepared but waiting for activation
================================================================================

The following code is intentionally unused, prepared for future features.
Each section documents what the code is waiting for.

## Phase 3.5: Trigger Execution
Location: internal/driver/compile_helpers.go
Functions:
  - executeBeforeInsertTriggers()
  - executeAfterInsertTriggers()
  - executeBeforeUpdateTriggers()
  - executeAfterUpdateTriggers()
  - executeBeforeDeleteTriggers()
  - executeAfterDeleteTriggers()
  - prepareNewRowForInsert()
Waiting for:
  - VDBE runtime integration (triggers execute during row ops, not compilation)
  - NEW/OLD row pseudo-tables populated from actual row data
  - Recursive statement execution within trigger body
See: compile_dml.go TODO Phase 3.5 comments

## Subquery Optimization
Location: internal/driver/compile_subquery.go
Functions:
  - adjustSubqueryJumpTargets()
  - buildJumpOpcodeMap()
  - buildDualJumpOpcodeMap()
  - adjustJumpTargetsInProgram()
  - compileInSubquery()
Location: internal/expr/codegen.go
  - registerAdjustmentRule (type)
  - registerAdjustmentRules (var)
  - adjustSubqueryCursors()
  - adjustSubqueryRegisters()
  - adjustInstructionRegisters()
Waiting for:
  - Subquery flattening optimization
  - Inline scalar subquery evaluation
  - Materialized IN subquery optimization

## CTE Bytecode Inlining
Location: internal/driver/stmt_cte.go
Functions:
  - compileCTEPopulation()
  - inlineCTEBytecode()
  - handleSpecialOpcode()
  - isControlFlowOp()
Waiting for:
  - CTE materialization optimization for multiple references
  - Recursive CTE support (iterative bytecode generation)
Currently using coroutine-based implementation (compileCTEPopulationCoroutine)

## GROUP BY Evaluation
Location: internal/driver/stmt_groupby.go
Functions:
  - evaluateGroupByExprs()
Waiting for:
  - Complex GROUP BY expressions requiring separate evaluation pass
Currently using inline bytecode generation in compileGroupBy

## Transaction Management
Location: internal/pager/transaction.go
Fields:
  - journal: For direct journal integration
  - readRefs: For multi-reader tracking
  - writeOwner: For deadlock detection
  - mu: For concurrent access protection
Waiting for:
  - WAL mode concurrent readers
  - Deadlock detection
  - Connection-level transaction isolation

## Pager Infrastructure
Location: internal/pager/pager.go
  - tempFile: For temp database support (ATTACH ':memory:')
  - changeCountDone: For file header change counter updates
Location: internal/pager/journal.go
  - headerSynced: For atomic commit tracking
Location: internal/pager/memory_pager.go
  - changeCountDone: For consistency with file-based pager
Location: internal/pager/savepoint.go
  - mu: For concurrent savepoint access
Location: internal/pager/wal_index.go
  - pageSize: For page size validation during WAL ops
Location: internal/pager/vacuum.go
  - allocateLocked(): For incremental/auto-vacuum modes

## Date/Time Functions
Location: internal/functions/date.go
  - isError: For error propagation in date parsing chains
  - getWeekday(): For strftime %w and %u format specifiers
  - getWeekNumber(): For strftime %W format specifier
  - getDayOfYear(): For strftime %j format specifier

## EXPLAIN Infrastructure
Location: internal/planner/explain.go
  - addOrderByNode(): Simplified version without cost estimates
  - formatIndexScan(): For detailed EXPLAIN with index info
Location: internal/planner/planner.go
  - explainLoop(): For EXPLAIN QUERY PLAN output

## R-Tree Virtual Table
Location: internal/vtab/rtree/rtree.go
  - queryBBox: For spatial range queries (overlap, contains, within)
  - queryType: For distinguishing spatial query operations

================================================================================
v0.1.x - Current Development
================================================================================

## Completed
[x] Core Infrastructure (B-tree, Pager, VDBE, Parser)
[x] SQL Features (Constraints, CTEs, Subqueries, DDL)
[x] Built-in Functions (String, Math, Date/Time, Aggregate)
[x] Query Optimization (Index selection, cost estimation)
[x] Code Quality (Cyclomatic complexity <= 9)
[x] Pre-commit validation (make commit)
[x] SPDX license headers on all files
[x] Documentation scaffolding comments

## In Progress
[x] Foreign Key Runtime Enforcement (83/83 tests passing) - COMPLETE
    - [DONE] FK manager wired through VDBE context
    - [DONE] INSERT/DELETE/UPDATE FK validation working
    - [DONE] CASCADE/SET NULL/SET DEFAULT/RESTRICT actions working
    - [DONE] PRAGMA foreign_key_check, DROP TABLE checks
    - [DONE] Deferred constraints, collation, type affinity
[~] CTE Edge Cases (improved)
    - [DONE] Sorter adjustment for GROUP BY in CTE coroutines
    - [~] Recursive CTE: works for small datasets (compile-time execution)
    - [ ] Recursive CTE bytecode generation (runtime loop optimization)
[x] WITHOUT ROWID Tables (45/51 tests passing)
    - [DONE] Composite PK key encoding (collation-aware, stable ordering)
    - [DONE] Btree cursor comparator for composite keys
    - [DONE] Split/rewind regression fix (statement cache invalidation)
    - [DONE] rowid column rejection, FK validation for WITHOUT ROWID
    - [DONE] ROLLBACK cache sync via pager callback
    - [DONE] CASCADE FK: PKColumnIndices for proper PK extraction
[x] VACUUM Operations (fixed)
    - [DONE] sqlite_master persistence with conditional commit
    - [DONE] Schema cookie management in commitTargetPager
    - [DONE] VACUUM INTO schema persistence with commit
[x] Window Functions - COMPLETE
    - [DONE] ROW_NUMBER, RANK, DENSE_RANK, NTILE
    - [DONE] LAG, LEAD, FIRST_VALUE, LAST_VALUE
    - [DONE] ROWS/RANGE frame specification parsing
[x] Sorter Spill-to-Disk - COMPLETE
    - [DONE] SorterInterface abstraction for sorter types
    - [DONE] SorterWithSpill: memory tracking, k-way merge, temp files
    - [DONE] VDBE integration via NewSorterWithSpillAndRegistry
    - [DONE] 10MB default memory limit, configurable

================================================================================
v0.2.0 - Planned Features
================================================================================

[ ] ANALYZE - Query optimizer statistics
    - Collect histogram data for table columns
    - Store in sqlite_stat1/sqlite_stat4 tables
    - Integrate with cost estimator

[ ] BACKUP - Online backup API
    - Incremental backup support
    - Progress callback
    - Concurrent read access during backup

[ ] FTS5 - Full-text search
    - Tokenizer infrastructure (internal/vtab/fts5/)
    - Segment storage/merge, search operators (MATCH, NEAR, prefix)
    - Ranking functions (bm25, etc.), highlight/offsets support

[ ] R-Tree - Spatial indexing
    - Bounding box queries (internal/vtab/rtree/)
    - Insert/Update/Delete operations and reinsertion/pack
    - Index maintenance and 2D/3D configurations

================================================================================
Future Phases
================================================================================

## Phase 4: Performance
[ ] Prepared statement caching
[ ] Connection pooling improvements
[ ] Memory limits and spill-to-disk for large sorts
[ ] Query plan caching

## Phase 5: Security
[ ] Input validation hardening
[ ] Resource limits enforcement
[ ] SQL injection prevention audit

## Phase 6: Testing
[ ] Fuzzing infrastructure
[ ] Benchmarks vs reference SQLite
[ ] Stress testing

## Phase 7: Configuration
[ ] DSN parsing improvements
[ ] Custom collation support
[ ] Extension loading

================================================================================
Known Limitations
================================================================================

- Windows file locking not implemented
- ORDER BY mixed ASC/DESC with NULLs has edge cases (basic ORDER BY works)
- ATTACH DATABASE partially implemented
- Window function edge cases (42 trinity tests skipped as pre-existing)
- Join edge cases (14 trinity tests skipped as pre-existing)
