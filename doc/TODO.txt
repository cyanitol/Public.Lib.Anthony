# Anthony - Pure Go SQLite Implementation - TODO
#
# Code Quality: Cyclomatic complexity <= 11 for all production code
# Coverage Snapshot: 100% trinity parity (1,257/1,257 tests passing)
# Last Updated: 2026-03-16

================================================================================
P0 — BIGGEST GAPS BY USER IMPACT
================================================================================

[ ] 1. Recursive CTE (WITH RECURSIVE)
    - [ ] Implement iterative VDBE loop for recursive term (OpInitCoroutine -> working queue)
    - [ ] Wire recursive self-reference detection in planner
    - [ ] Handle UNION vs UNION ALL semantics in recursive step
    - [ ] Cycle detection / depth limit (SQLITE_MAX_RECURSIVE_DEPTH = 1000)
    - [ ] Test: hierarchical data (org chart), graph traversal, sequence generation
    Files: internal/driver/stmt_cte_recursive.go, internal/planner/cte.go, internal/vdbe/exec.go

[ ] 2. RETURNING clause (INSERT/UPDATE/DELETE RETURNING)
    - [ ] Add Returning []ResultColumn field to InsertStmt, UpdateStmt, DeleteStmt AST
    - [ ] Parse RETURNING column list after DML statement
    - [ ] Compile RETURNING as result-row emission after each DML row operation
    - [ ] Handle RETURNING * expansion
    - [ ] Handle RETURNING expressions (function calls, aliases)
    Files: internal/parser/ast.go, internal/parser/parser.go, internal/driver/compile_dml.go

[ ] 3. WAL mode (Write-Ahead Logging)
    - [ ] Implement WAL frame writing (wal.go already has read path)
    - [ ] WAL checkpoint (PASSIVE, FULL, RESTART, TRUNCATE modes)
    - [ ] Concurrent reader support (shared memory WAL index)
    - [ ] PRAGMA journal_mode=WAL switching
    - [ ] WAL recovery on open (replay uncommitted frames)
    Files: internal/pager/wal.go, internal/pager/wal_index.go, internal/pager/transaction.go

[ ] 4. FTS5 SQL integration
    - [ ] Wire FTS5 module into SQL execution pipeline (CREATE VIRTUAL TABLE ... USING fts5)
    - [ ] Compile MATCH operator for FTS5 queries in WHERE clauses
    - [ ] Route INSERT/UPDATE/DELETE through FTS5 module update method
    - [ ] Expose rank(), snippet(), highlight() as SQL functions
    - [ ] Shadow table persistence (content, data, idx, config tables)
    Files: internal/vtab/fts5/, internal/driver/compile_select.go, internal/driver/compile_dml.go

[ ] 5. generate_series TVF
    - [ ] Implement generate_series(start, stop, step?) table-valued function
    - [ ] Register in TVF dispatch alongside json_each/json_tree
    - [ ] Support all three argument forms (start, start+stop, start+stop+step)
    Files: internal/functions/generate_series.go, internal/driver/compile_tvf.go

================================================================================
P1 — PARTIALLY IMPLEMENTED
================================================================================

[ ] 6. RIGHT / FULL OUTER JOIN execution
    - [ ] Add RIGHT JOIN compilation path (swap left/right, emit LEFT JOIN, reorder columns)
    - [ ] Add FULL OUTER JOIN compilation (LEFT JOIN + anti-join of right unmatched rows)
    - [ ] Handle NULL padding for unmatched rows on both sides
    Files: internal/driver/compile_join.go

[ ] 7. Generated columns execution
    - [ ] Evaluate GENERATED ALWAYS AS expressions during INSERT/UPDATE
    - [ ] Enforce STORED vs VIRTUAL semantics (stored persists to B-tree, virtual computed on read)
    - [ ] Prevent direct writes to generated columns
    Files: internal/driver/compile_dml.go, internal/schema/table.go

[ ] 8. VACUUM hardening
    - [ ] Fix schema persistence issues causing 30 skipped tests
    - [ ] Incremental vacuum support (auto_vacuum=INCREMENTAL)
    - [ ] VACUUM INTO with proper index rebuilding
    Files: internal/pager/vacuum.go, internal/driver/compile_ddl.go

[ ] 9. WITHOUT ROWID remaining 6 tests
    - [ ] Investigate and fix remaining 6 skipped tests
    - [ ] Likely edge cases in ROLLBACK, complex CASCADE, or composite PK ordering
    Files: internal/withoutrowid/, internal/driver/compile_dml.go

[ ] 10. EXPLAIN QUERY PLAN completeness
    - [ ] Full tree output matching SQLite format (SCAN, SEARCH, USE TEMP B-TREE)
    - [ ] Index usage reporting
    - [ ] Subquery and compound query plan nodes
    Files: internal/planner/explain.go

================================================================================
P2 — NOT IMPLEMENTED (REMAINING)
================================================================================

[ ] 11. UPDATE...FROM
    - [ ] Add From *FromClause to UpdateStmt AST
    - [ ] Parse FROM clause after SET in UPDATE
    - [ ] Compile as join between target table and FROM sources
    Files: internal/parser/ast.go, internal/parser/parser.go, internal/driver/compile_dml.go

[ ] 12. PERCENT_RANK / CUME_DIST window functions
    - [ ] Implement PERCENT_RANK: (rank - 1) / (partition_size - 1)
    - [ ] Implement CUME_DIST: count_of_rows_with_value_le / partition_size
    - [ ] Add opcodes or compute in window evaluation loop
    Files: internal/driver/compile_select_agg.go, internal/vdbe/exec.go

[ ] 13. ANALYZE
    - [ ] Create sqlite_stat1 / sqlite_stat4 tables on ANALYZE
    - [ ] Collect row count and index selectivity statistics
    - [ ] Integrate stats into query planner cost estimator
    Files: internal/driver/compile_ddl.go, internal/planner/cost.go, internal/schema/

[ ] 14. trunc() math function
    - [ ] Implement trunc(X) and trunc(X,Y) — truncate toward zero
    - [ ] Register in math function table
    Files: internal/functions/math.go

[ ] 15. likelihood / likely / unlikely optimizer hints
    - [ ] Parse as no-op functions (pass through argument unchanged)
    - [ ] Optionally wire into cost estimator for selectivity hints
    Files: internal/functions/builtin.go, internal/parser/parser.go

[ ] 16. Custom collations
    - [ ] Expose CollationRegistry.Register() through database/sql driver
    - [ ] Support CREATE COLLATION or driver-level API
    - [ ] Ensure collation propagation through indexes and comparisons
    Files: internal/collation/collation.go, internal/driver/driver.go

[ ] 17. R-Tree SQL integration
    - [ ] Wire R-Tree module into SQL pipeline (CREATE VIRTUAL TABLE ... USING rtree)
    - [ ] Route spatial queries through R-Tree module
    - [ ] Persistent storage to database file
    Files: internal/vtab/rtree/, internal/driver/compile_select.go

[ ] 18. IS DISTINCT FROM / IS NOT DISTINCT FROM operators
    - [ ] Add BinaryOp variants (OpIsDistinctFrom, OpIsNotDistinctFrom)
    - [ ] Parse IS DISTINCT FROM and IS NOT DISTINCT FROM syntax
    - [ ] Compile as NULL-safe inequality/equality
    Files: internal/parser/ast.go, internal/parser/parser.go, internal/expr/codegen.go

================================================================================
P3 — PLATFORM & HARDENING
================================================================================

[ ] 19. Windows file locking
    - [ ] Implement LockFile/UnlockFile via syscall on Windows
    - [ ] Test concurrent access on Windows
    Files: internal/pager/lock_windows.go

[ ] 20. BACKUP API
    - [ ] Implement online backup (page-by-page copy with concurrent read)
    - [ ] Progress callback support
    Files: internal/pager/backup.go

[ ] 21. Durability & conformance hardening
    - [ ] Run sqllogictest subsets vs reference SQLite
    - [ ] Fuzz parser and record decoder
    - [ ] Crash-replay tests for WAL/journal
    - [ ] Target >=80% coverage in pager/vdbe/btree/parser/engine

================================================================================
COMPLETED (v0.1.x - v0.3.0)
================================================================================

[x] Core Infrastructure (B-tree, Pager, VDBE, Parser)
[x] SQL Features (Constraints, CTEs, Subqueries, DDL)
[x] Built-in Functions (String, Math, Date/Time, Aggregate, JSON)
[x] Query Optimization (Index selection, cost estimation)
[x] Foreign Key Runtime (83/83 tests passing)
[x] WITHOUT ROWID Tables (45/51 passing)
[x] Window Functions (ROW_NUMBER, RANK, DENSE_RANK, NTILE, LAG, LEAD, FIRST_VALUE, LAST_VALUE, NTH_VALUE)
[x] Triggers (BEFORE/AFTER x INSERT/UPDATE/DELETE)
[x] ALTER TABLE (RENAME TABLE, RENAME COLUMN, DROP COLUMN)
[x] ATTACH/DETACH DATABASE
[x] JSON TVFs (json_each, json_tree)
[x] FTS5 Module (128 tests, API level)
[x] R-Tree Module (all tests, API level)
[x] Sorter Spill-to-Disk
[x] Trinity Test Parity (1,257/1,257)
